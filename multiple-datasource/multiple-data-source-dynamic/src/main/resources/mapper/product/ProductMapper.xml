<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.feng.datasource.multiple.dynamic.repository.product.ProductRepository" >

  <resultMap id="BaseResultMap" type="com.feng.datasource.multiple.dynamic.entity.Product">
    <result column="id" property="id" jdbcType="INTEGER"/>
    <result column="code" property="code" jdbcType="VARCHAR"/>
    <result column="name" property="name" jdbcType="VARCHAR"/>
    <result column="price" property="price" jdbcType="DOUBLE"/>
  </resultMap>

  <!--查询-->
  <select id="find" parameterType="BaseResultMap" resultMap="BaseResultMap">
    SELECT id,code,`name`,price
    FROM t_product
    <trim prefix="WHERE" prefixOverrides="AND | OR">
      <if test="id != null">
        AND id = #{id}
      </if>
      <if test="code != null and code != ''">
        AND code = #{code}
      </if>
      <if test="name != null and name != ''">
        AND `name` = #{name}
      </if>
      <if test="price != null">
        AND price = #{price}
      </if>
    </trim>
  </select>
</mapper>
